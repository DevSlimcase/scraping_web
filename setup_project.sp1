# setup_project.sp1

# This script sets up the project dependencies for both Node.js and Python environments.

# Install Node.js dependencies
npm install

# Install Python dependencies
pip install -r convert/requirements.txt
